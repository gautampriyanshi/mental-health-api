services:
  - type: web
    name: mental-health-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn mental_health_api.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: mental_health_api.settings
build:
  pythonVersion: 3.10

